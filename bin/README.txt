Authors Note: 

Just click the runMe.bat to run the program but be sure
to set the java classpath to where the java/bin directory is located.

A splash screen should appear if not then check the path...